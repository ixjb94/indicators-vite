/**
 * More efficient
 * @param {number[]} source example: sma | rsi | cci | etc.
 * @param {number} length main source length, example: close.length
 * @returns
 */
export function normalize2(source: number[], length: number): number[];
