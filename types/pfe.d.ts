/**
 *
 * @param {number[]} source
 * @param {number} period
 * @param {number} ema_period
 * @param {number} [size]
 * @returns
 */
export function pfe(source: number[], period: number, ema_period: number, size?: number): number[];
